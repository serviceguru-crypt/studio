
import { LandingPage } from '@/app/landing-page';

// This is a server component by default.
// It will render the LandingPage component.
export default async function Page() {
  return <LandingPage />;
}
